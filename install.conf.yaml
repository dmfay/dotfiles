- clean: ['~']

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - mkdir -p ~/.config
    - mkdir -p ~/.when

- link:
    ~/.zshenv: zshenv
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.xinitrc: xinitrc
    ~/.xmodmap: xmodmap
    ~/.config/rofi: rofi/
    ~/.config/cmus/caput-mortuum.theme: cmus/caput-mortuum.theme
    ~/.config/dunst: dunst/
    ~/.config/fontconfig: fontconfig/
    ~/.config/i3: i3/
    ~/.config/mutt: mutt/
    ~/.config/nvim: vim
    ~/.config/nvim/init.vim: vimrc
    ~/.config/polybar: polybar/
    ~/.config/termite: termite/
    ~/.config/compton.conf: compton.conf
    ~/.muttrc: muttrc
    ~/.psqlrc: psqlrc
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.when/preferences: when/preferences
    ~/.zsh: zsh/
